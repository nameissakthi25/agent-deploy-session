# Pinned to exact versions. No ranges, no "latest" -- a resolver surprise on
# the day of the session cannot be recovered from.
# Verified against PyPI on 2026-09-09.

# Runtime: imported by the app containers.
fastapi==0.141.1
uvicorn==0.52.4
openai==3.10.0
pydantic==2.13.5
httpx==0.28.1

# Tracing. arize-phoenix-otel is the client half; the Phoenix server runs as
# its own container, so the full arize-phoenix package is not needed here.
arize-phoenix-otel==0.17.1
openinference-instrumentation-openai==0.1.59
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0

# Retrieval. fastembed is ONNX on CPU -- no torch, and it keeps embedding off
# the GPU so the whole card stays available for generation.
qdrant-client==1.19.0
fastembed==0.8.0

# The Stage 3 graph.
langgraph==1.2.11
langchain-core==1.6.2

# Tests.
pytest==9.1.1

# Build-time only: used by scripts/, not imported by the app containers.
huggingface_hub==1.9.2
pandas==2.3.3
pyarrow==22.0.0
